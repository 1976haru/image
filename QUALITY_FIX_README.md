# CoverMorph 복원 수정판

이 압축은 전체 Python 소스입니다. Windows EXE나 AI 모델을 포함하지 않습니다.
기존 프로젝트에 덮어쓰기 전에 별도 폴더에 압축을 풀어 실행하세요.

## 설치
1. Python 3.11 또는 3.12를 설치합니다.
2. INSTALL.bat 실행: 기본 프로그램 설치.
3. INSTALL_AI_LAMA.bat 실행: 별도 .venv_lama에 LaMa 설치, 모델 다운로드 및 작은 이미지 추론 검사.
4. AI 자연 배경 확장이 필요하면 INSTALL_AI_OUTPAINT.bat 실행.
5. RUN.bat 실행. 일본어 커버는 일본어+영어 OCR을 선택합니다.

LaMa 설치는 기본 환경의 NumPy/Pillow를 낮추지 않습니다. 이미 충돌이 있는 이전 환경을
재사용하지 않도록 새 폴더에서 설치하세요. SDXL은 첫 변환 때 큰 모델을 다운로드합니다.
CPU 실행은 매우 오래 걸릴 수 있습니다. 실제 상태는 작업 JSON과 오류 로그에서 확인하세요.

## 수정 내용
- 큰 OCR 영역을 OpenCV로 지워 줄무늬가 발생하는 경로를 차단합니다.
- OCR 마스크 여백을 글자 높이/폭 중 작은 치수 기준으로 줄였습니다.
- LaMa 결과의 마스크 바깥은 원본 픽셀을 그대로 보존합니다.
- 배경 가장자리의 얇은 띠를 길게 늘리는 처리를 제거했습니다.
- AI 실패 시 로컬 반사/블러 배경을 AI 성공으로 저장하지 않습니다.
- SDXL 추론에 가로/세로 크기를 명시하고, 생성 대상 이외의 픽셀을 복원합니다.
- 인물 보호 합성 시 색상이 두 번 투명해지는 오류를 수정했습니다.
- AI 미리보기는 실제 저장된 결과를 표시합니다. 생성 전에는 안내를 표시합니다.
- 글자 제거 실패도 작업 JSON에 남기고 해당 이미지의 결과 저장을 중단합니다.

## 사용과 한계
스마트 크롭은 화면을 채우기 위해 원본의 일부를 자릅니다. 원본 전체 보존과 화면 채우기를
함께 원하면 실제 AI 배경 확장이 필요합니다. 반사 배경은 생성된 자연 배경이 아닙니다.
글자가 인물/복잡한 물체를 가린 경우 AI가 가려진 원래 내용을 정확히 복구할 수는 없습니다.
자동 OCR 누락은 마스크 미리보기에서 확인하고 수동 영역으로 추가하세요.

## 검증 범위
이 환경에서는 실제 LaMa/SDXL 모델과 Windows GUI를 실행하지 못했습니다.
자동 테스트의 가짜 AI는 출력 연결과 오류 처리만 검증하며 복원 품질을 증명하지 않습니다.
따라서 이 배포물을 실제 이미지 복원 품질 검증이 끝난 완성판으로 표시하지 않습니다.
RUN_TESTS.bat으로 회귀 검사를 실행할 수 있습니다.
